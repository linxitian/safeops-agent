schema_version: 1
allowed_services:
  - safeops-demo-web.service
read_file_roots:
  - /
allowed_file_roots:
  - /home
  - /var/lib/safeops/quarantine
lab_file_roots:
  - /home
quarantine_root: /var/lib/safeops/quarantine
allowed_process_executables:
  - /opt/safeops/bin/safeops-port-holder
  - /opt/safeops/bin/safeops-cpu-hog
  - /opt/safeops/bin/safeops-log-writer
